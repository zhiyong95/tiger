[project]
requires = ["nodejs-24"]

[dev]
build = ["pnpm", "install"]
run = ["pnpm", "run", "dev"]

[deploy]
build = ["sh", "-c", "pnpm install && pnpm run build"]
run = ["sh", "-c", "npx serve -l ${DEPLOY_RUN_PORT} dist"]
